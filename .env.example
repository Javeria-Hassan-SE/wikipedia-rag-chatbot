# Ollama service (matches docker-compose service name)
OLLAMA_BASE_URL=http://ollama:11434

# Qdrant service (matches docker-compose service name)
QDRANT_HOST=qdrant
QDRANT_PORT=6333

# Models — must be available in the Ollama container
EMBED_MODEL=all-minilm
CHAT_MODEL=llama3.2:3b
